<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.apidata">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".DashboardActivity" android:label="DashBoard">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UpdateDeleteActivity" android:label="Update/Delete Employee"></activity>
        <activity android:name=".RegisterEmployeeActivity" android:label="Register Employee"/>
        <activity android:name=".SearchEmployeeActivity" android:label="Search Employee"></activity>
        <activity android:name=".MainActivity" android:label="Display All Employee"/>
    </application>

</manifest>